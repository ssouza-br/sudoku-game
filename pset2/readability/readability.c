#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string text = get_string("Text: ")
}

int count_letters(string text)
{
    int len = strlen(text);
    int counter_letters = 0;
    for (i = 0; i < len; i++)
    {
        if (text[i] = )
    }
}